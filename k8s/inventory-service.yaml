apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: vishal101090/inventory-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVER_PORT
              value: "5005"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://discovery-service:5001/eureka/"
            - name: SPRING_DATA_MONGODB_URI
              value: "mongodb://mongodb-service:27017/inventory-db"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka-service:9092"
          ports:
            - containerPort: 5005
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 5005
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 5005
            initialDelaySeconds: 40
            periodSeconds: 10

---

apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  selector:
    app: inventory-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005

---

#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: inventory-ingress
#spec:
#  ingressClassName: nginx
#  rules:
#    - host: inventory.local
#      http:
#        paths:
#          - path: /
#            pathType: Prefix
#            backend:
#              service:
#                name: inventory-service
#                port:
#                  number: 5005
